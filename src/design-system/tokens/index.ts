export { defaultTheme } from '@/utils/styles.base';
export type { Theme } from '@/utils/styles.types';

export {
  colors,
  typography,
  spacing,
  borderRadius,
  shadows,
  opacity,
  motion,
  transitions,
  zIndex,
  icons,
  components,
  getBadgeColor,
  getStatusColor,
  getStatusIcon,
} from '@/lib/design-tokens';

export { platformOverrides } from './platform';
